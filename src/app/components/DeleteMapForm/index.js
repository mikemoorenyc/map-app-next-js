import Button from "../Button"
import { deleteMap } from "@/app/actions/maps"

export default async function DeleteMapForm({id}) {
  const submitter = (e) => {
    
  }

  return <form>
  dafsdzs
  </form>
}